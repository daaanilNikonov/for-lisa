# Квиз «1С-ЭПД / Доки.Логистика»

Исправленная версия презентации `Квиз_1С-ЭПД_Доки_Логистика ИИ ПОПРАВИТЬ.pptx`.

## Что исправлено

1. **Таймер / ответ** — ответ появляется через **40 секунд** (было 30; выходил ~на 10 с раньше визуального таймера). Анимация пересобрана с `bldLst` (карточка скрыта до появления).
2. **Открытые ячейки** — цвет балла меняется через стандартный механизм PowerPoint **followed hyperlink**: неоткрытые — белые, открытые — красные (`#C0392B`). Работает в режиме показа слайдов при возврате на поле по гиперссылке «← К полю».

Тексты и фигуры, добавленные вручную, не откатывались.

## Пересборка патча

```bash
python3 scripts/fix_epd_quiz_pptx.py
```
